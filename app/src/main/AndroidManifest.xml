<?xml version="1.0" encoding="utf-8"?>
<!--
  SentinelVault - 100% Offline Edge-AI Security App.
  Zero-Internet Policy: the INTERNET permission is intentionally NOT declared.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      The CAMERA permission implicitly declares android.hardware.camera as required.
      We mark it required="false" so the APK remains installable on devices without a
      generic/back camera (e.g. ChromeOS); the front camera is what we actually use and
      its absence is handled gracefully at runtime by the enrollment flow.
    -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <!-- Epic 9 / Task 9.2: Android 13+ runtime permission for the persistent FGS notification.
         The service still works without it, but the user has no visibility into its state. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SentinelApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SentinelVault"
        android:usesCleartextTraffic="false"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.SentinelVault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Device Admin receiver. Full lockdown lands in Epic 6; declared here so the
             onboarding flow (Epic 2) can deep-link the user to "Activate device admin". -->
        <receiver
            android:name=".security.admin.SentinelDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Epic 4: passive trigger for "device just unlocked". ACTION_USER_PRESENT is in
             the Android 8+ implicit-broadcast exception list, so manifest registration is
             still permitted without a sticky service. -->
        <receiver
            android:name=".triggers.UserPresentReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- Epic 4: push-based foreground tracker. The user must enable Sentinel under
             Settings → Accessibility before this fires. -->
        <service
            android:name=".triggers.SentinelAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Epic 9 / Task 9.1: foreground LifecycleService that owns the headless camera
             session and the SentinelRuntime. `stopWithTask=false` is critical so Samsung's
             Recents-swipe call to onTaskRemoved does NOT end the service alongside the task. -->
        <service
            android:name=".service.SentinelVigilanceService"
            android:exported="false"
            android:foregroundServiceType="camera"
            android:stopWithTask="false" />

        <!-- Epic 9 / Task 9.5: re-arms the vigilance service on reboot. `exported=true` is
             required because the Android system process is the broadcast sender; this is one
             of the documented exceptions to the Android 12 receiver-export rule. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
